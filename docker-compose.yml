version: '3'

services:
  proxy:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
  backend:
    build: ./backend
  frontend:
    image: cloudmap-frontend:prod
    build: ./frontend
